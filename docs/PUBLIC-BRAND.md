# Public brand

The public product name is **OPTIANZA**.

Technical identifiers intentionally retained for compatibility may still use `ElegiBien` in project names, namespaces, database context names, migration history, configuration keys, and internal paths. These identifiers are implementation details and are not the public brand.

Public domain: `optianza.com.ar`

Public contact email: `contacto@optianza.com.ar`
